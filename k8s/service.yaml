apiVersion: v1
kind: Service
metadata:
  name: northwind-delivery
  labels:
    app: northwind-delivery
spec:
  selector:
    app: northwind-delivery
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: LoadBalancer
